<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EJS Tags</title>
</head>

<body>
  <h1>
    <%= new_data["title"] %>
  </h1>
  <p>Current second:
    <%= new_data["seconds"] %>
  </p>
  
  <%  if (new_data["seconds"] % 2===0 )  { %>
  <ul>
    <% for (let index = 0; index < new_data["items"].length; index++) { %>
      
      <li>
        <%= new_data["items"][index] %>
      </li>
      
    <%   } %>
    <!-- List goes here if current second is even. -->
    <!-- Otherwise it should display the following: -->
    <!-- <p>No items to display</p> -->
  </ul>
  <% }  else { %>
  <p>No items to display</p>
  <% } %>
  <p>
    <%- new_data["htmlContent"]%>
  </p>

  <%- include("footer.ejs")  %>
</body>

</html>